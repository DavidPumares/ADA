with Figura_Model; use Figura_Model;
with Cuadrado; use Cuadrado;
with Figura_Model; use Figura_Model;

procedure Main is

   Figura1 : Rectangulo := Create(Nombre => "Rect",
                                  Base => 6.0,
                                  Altura => 2.0);
begin
   --  Crear una clase abstract afigura
   --  Crear el metodo abstracto calcular area
   -- Crear la subclase cuadrado que herede de figura
   -- Sobrescribir el metodo calcular area
   null;

end Main;
